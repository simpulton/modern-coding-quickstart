# Secret used to sign workshop JWTs. Override in your own environment.
JWT_SECRET=dev-secret-change-me

# drizzle-kit credentials. The app/tests use in-memory PGLite, so this is only
# needed if you point drizzle-kit at a real Postgres.
DATABASE_URL=memory://
